import React from 'react';
import { RouteComponentProps } from '@reach/router';

export default class MyCompTwo extends React.Component<RouteComponentProps> {
  render(): JSX.Element {
    return (
      <div>My Comp Two</div>
    );
  }
}